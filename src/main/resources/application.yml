spring:
  data:
    mongodb:
      auto-index-creation: true
      uri: mongodb://${MONGODB_HOSTNAME}:27017/flowfully-user-lambda-db

mongodb:
  encrypt:
    keys:
      - version: 1
        key: ${MONGODB_ENCRYPTION_KEY}